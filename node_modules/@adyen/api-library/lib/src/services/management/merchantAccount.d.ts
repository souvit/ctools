import Service from "../../service";
import { CreateMerchantRequest, CreateMerchantResponse, ListMerchantResponse, Merchant, RequestActivationResponse } from "../../typings/management/models";
import { IRequest } from "../../typings/requestOptions";
declare class MerchantAccount extends Service {
    /**
     * Get a list of merchant accounts
     */
    list(requestOptions?: IRequest.Options): Promise<ListMerchantResponse>;
    /**
     * Create a merchant account
     */
    create(request: CreateMerchantRequest): Promise<CreateMerchantResponse>;
    /**
     * Get a merchant account
     */
    retrieve(merchantId: string): Promise<Merchant>;
    /**
     * Request to activate a merchant account
     */
    activate(merchantId: string): Promise<RequestActivationResponse>;
}
export default MerchantAccount;
