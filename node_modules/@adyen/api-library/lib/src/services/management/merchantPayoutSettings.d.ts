import Service from "../../service";
import { PayoutSettings, PayoutSettingsRequest, PayoutSettingsResponse, UpdatePayoutSettingsRequest } from "../../typings/management/models";
declare class MerchantPayoutSettings extends Service {
    /**
     * Get a list of payout settings
     */
    listPayoutSettings(merchantId: string): Promise<PayoutSettingsResponse>;
    /**
     * Add a payout setting
     */
    create(merchantId: string, request: PayoutSettingsRequest): Promise<PayoutSettings>;
    /**
     * Delete a payout setting
     */
    delete(merchantId: string, payoutSettingsId: string): Promise<void>;
    /**
     * Get a payout setting
     */
    retrieve(merchantId: string, payoutSettingsId: string): Promise<PayoutSettings>;
    /**
     * Update a payout setting
     */
    update(merchantId: string, payoutSettingsId: string, request: UpdatePayoutSettingsRequest): Promise<PayoutSettings>;
}
export default MerchantPayoutSettings;
