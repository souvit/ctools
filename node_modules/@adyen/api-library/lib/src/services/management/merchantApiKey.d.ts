import Service from "../../service";
import { GenerateApiKeyResponse } from "../../typings/management/models";
declare class MerchantApiKey extends Service {
    /**
     * Generate new API key
     */
    create(merchantId: string, apiCredentialId: string): Promise<GenerateApiKeyResponse>;
}
export default MerchantApiKey;
