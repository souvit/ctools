import Service from "../../service";
import { GenerateClientKeyResponse } from "../../typings/management/models";
declare class MerchantClientKey extends Service {
    /**
     * Generate new client key
     */
    create(merchantId: string, apiCredentialId: string): Promise<GenerateClientKeyResponse>;
}
export default MerchantClientKey;
