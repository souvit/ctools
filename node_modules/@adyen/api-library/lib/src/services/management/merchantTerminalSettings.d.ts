import Service from "../../service";
import { Logo, TerminalSettings } from "../../typings/management/models";
import { IRequest } from "../../typings/requestOptions";
declare class MerchantTerminalSettings extends Service {
    /**
     * Get the terminal logo
     */
    retrieveLogo(merchantId: string, requestOptions?: IRequest.Options): Promise<Logo>;
    /**
     * Update the terminal logo
     */
    updateLogo(merchantId: string, request: Logo, requestOptions?: IRequest.Options): Promise<Logo>;
    /**
     * Get terminal settings
     */
    retrieve(merchantId: string): Promise<TerminalSettings>;
    /**
     * Update terminal settings
     */
    update(merchantId: string, request: TerminalSettings): Promise<TerminalSettings>;
}
export default MerchantTerminalSettings;
