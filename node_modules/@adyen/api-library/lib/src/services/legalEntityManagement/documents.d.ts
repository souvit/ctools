import Service from "../../service";
import { Document } from "../../typings/legalEntityManagement/models";
export type DocumentUpdate = Omit<Document, "id">;
declare class Documents extends Service {
    create(request: DocumentUpdate): Promise<Document>;
    retrieve(id: string): Promise<Document>;
    update(id: string, request: DocumentUpdate): Promise<Document>;
    delete(id: string): Promise<void>;
}
export default Documents;
