import Service from "../../service";
import { BusinessLine, BusinessLineInfo, BusinessLineInfoUpdate } from "../../typings/legalEntityManagement/models";
declare class BusinessLineService extends Service {
    create(request: BusinessLineInfo): Promise<BusinessLine>;
    retrieve(id: string): Promise<BusinessLine>;
    update(id: string, request: BusinessLineInfoUpdate): Promise<BusinessLine>;
}
export default BusinessLineService;
