import Client from "../client";
import Service from "../service";
import { TransferInfo, Transfer, Transaction, TransactionSearchResponse } from "../typings/transfer/models";
declare class Transfers extends Service {
    private readonly _transfersResource;
    private readonly _listTransactionsResource;
    constructor(client: Client);
    transfers(request: TransferInfo): Promise<Transfer>;
    getTransaction(id: string): Promise<Transaction>;
    listTransactions(): Promise<TransactionSearchResponse>;
}
export default Transfers;
